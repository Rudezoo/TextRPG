#pragma once


enum MAIN_MENU {
	MM_NONE = 0,
	MM_MAP,
	MM_STORE,
	MM_INVENTORY,
	MM_STAT,
	MM_EXIT
};

enum MAP_TYPE {
	MT_NONE = 0,
	MT_EASY,
	MT_NORMAL,
	MT_HARD,
	MT_BACK
};

enum JOB
{
	JOB_NONE = 0,
	JOB_KNIGHT,
	JOB_WIZARD,
	JOB_ARCHER,
	JOB_ROGUE,
	JOB_END
};

enum BATTLE
{
	BATTLE_NONE = 0,
	BATTLE_FIGHT,
	BATTLE_RUN
};

enum ITEM_TYPE {
	IT_NONE = 0,
	IT_WEAPON,
	IT_ARMOR,
	IT_BACK,
};


#define MAX_INVENTORY 20
#define STORE_ARMOR_MAX 3
#define STORE_WEAPON_MAX 3
#define WEAPON_MAX 20
#define ARMOR_MAX 20
#define MAX_EQUIPPED 2


enum STORE_MENU {
	SM_NONE = 0,
	SM_WEAPON,
	SM_ARMOR,
	SM_BACK,
};

enum IN_MENU {
	IN_NONE = 0,
	IN_EQUIP,
	IN_UNEQUIP,
	IN_BACK,
};
